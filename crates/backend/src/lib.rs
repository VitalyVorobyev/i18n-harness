//! `TranslationBackend` trait and backend implementations.
//!
//! See `docs/initial_design.md` §5. Phase 0 stub — trait and `manual`
//! backend land in M2 alongside `ollama`; `openai-compatible` is M4.
//! The "agent" path is a two-phase CLI (see `crates/cli`), not a backend.
