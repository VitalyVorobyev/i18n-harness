//! `CatalogFormat` trait and serializers.
//!
//! See `docs/initial_design.md` §6. Phase 0 stub — trait and PO/ICU-JSON
//! serializers land in M4. Qt is its own adapter (`adapter-qt`) because the
//! parser dominates; the React side runs through this crate's serializers.
