[package]
name = "i18n-harness-locales"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Locale records and CLDR plural-arity tables as data, not match arms."

[lints]
workspace = true

# Locale data is a static Rust table (see lib.rs doc comment); no serialization
# dependency is needed at this stage. If the table moves to YAML/TOML later, add
# `serde` + the loader of choice here.
[dependencies]
